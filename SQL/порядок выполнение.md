# порядок выполнения операторов

```bash
FROM/JOIN
    |
    v
  WHERE
    |
    v
 GROUP BY
    |
    v
 DISTINCT
    |
    v
  SELECT
    |
    v
  HAVING
    |
    v
 ORDER BY
    |
    v
LIMIT/OFFSET
```
